# Find KU Dae — Design System

เอกสารนี้คือข้อตกลงเรื่องหน้าตาของทั้งเว็บ ทุกหน้าใหม่ต้องเดินตามนี้
ถ้าจะแหกกฎข้อไหน ให้แก้เอกสารนี้ก่อน แล้วค่อยแก้โค้ด

---

## 0. สิ่งที่ถูกยกเลิกไปแล้ว — ห้ามกลับมา

เว็บนี้เคยมีระบบ "สองโลก" (ช่องมองภาพมืด + โลกสว่าง), กรอบ autofocus, และแถบเขียวมะนาวขีดใต้หัวข้อ
**เจ้าของยกเลิกทั้งหมดแล้ว** ถ้าเจอของพวกนี้ในโค้ด แปลว่าตกค้าง ให้ลบ

- ❌ กรอบ AF / `.af` / `.af-corner`
- ❌ แถบขีดใต้หัวข้อ `.rule`
- ❌ พื้นหลังเขียวเกือบดำ `viewfinder`
- ❌ อุปมาอุปไมยเชิงแนวคิด (แผ่นคอนแทค, หนังสือรุ่น, ช่องมองภาพ)

---

## 1. แนวคิดหลัก — ขนบ ทำให้ดีที่สุด

เจ้าของเลือก**ขนบมาตรฐานของหมวดนี้อย่างตั้งใจ** เมื่อถูกเสนอทางที่แปลกใหม่กว่า
เว็บนี้จึงต้องหน้าตาเหมือนบริการค้นรูปจากงานอีเวนต์ที่คนใช้เป็นอยู่แล้ว

**เหตุผลที่ขนบถูกสำหรับโปรดักต์นี้:** ผู้ใช้ยืนตากแดดกลางงาน สแกน QR ด้วยมือถือ
มาครั้งเดียว ไม่เคยเห็นเว็บนี้มาก่อน และไม่มีโอกาสเรียนรู้อินเทอร์เฟซใหม่
รูปแบบที่แปลกคือภาระของเขา ไม่ใช่ของขวัญ

**เกณฑ์วัดฝีมือที่เจ้าของกำหนด**

| อ้างอิง | เอาอะไรมา |
|---|---|
| **Sportograf / FotoOwl** | โครงสร้าง ลำดับการอ่าน ความตรงไปตรงมา ความน่าเชื่อถือ |
| **LINE / แอปคนไทย** | อุณหภูมิ สีสด มุมมนใหญ่ คำพูดเป็นกันเอง ไม่มีพิธีรีตอง |

**ความขี้เล่นอยู่ที่การลงมือทำ ไม่ใช่ที่รูปแบบ** — สี จังหวะ คำพูด และ micro-interaction
ทำให้ขี้เล่นได้เต็มที่ แต่โครงหน้ายังเป็นโครงที่คนคุ้นเคย
ห้ามยัดลูกเล่นเชิงแนวคิดกลับเข้ามาแบบเสียดสีหรือแอบ ๆ

---

## 2. สี — สองสีจากโลโก้ จบ

เจ้าของสั่งชัดว่า **"เน้นสีให้เหมือนโลโก้"** ระบบสีจึงมีสีแบรนด์แค่สองสี
ไม่มีสีเสริม ไม่มีสี tag แยกหมวด ไม่มี gradient หลากสี
ความสดใสมาจาก**บล็อกสีก้อนใหญ่ + ขาวเยอะ + เขียวมะนาวจุดเดียว**

### ค่าจากโลโก้

| | Hex | มาจากไหน |
|---|---|---|
| **KU Teal** | `#00706b` | ตัวอักษร KU |
| **KU Lime** | `#b8c214` | แถบใต้ตัวอักษร |

> ⚠️ สองค่านี้อ่านจากภาพโลโก้ด้วยตา ยังไม่ได้ดูดจากไฟล์จริง
> ทั้งสองอยู่ที่ `--color-green-600` และ `--color-lime-500` ใน `globals.css`
> **มีที่ซ้ำอยู่หนึ่งจุดที่หลีกเลี่ยงไม่ได้:** `themeColor` ใน `app/layout.tsx`
> เพราะ metadata ของ Next อ่านค่า CSS custom property ไม่ได้ ต้องแก้พร้อมกันเสมอ

### สเกลเขียว (ไล่จาก KU Teal)

`green-50 #e9f7f5` · `100 #c6ece8` · `200 #93dbd4` · `300 #57c3ba` · `400 #22a79c`
`500 #068a80` · **`600 #00706b` ← แบรนด์** · `700 #005a57` · `800 #014746` · `900 #05393a` · `950 #01211f`

| ใช้ตรงไหน | Token |
|---|---|
| ปุ่มหลัก แถบเต็มความกว้าง หัวข้อบนพื้นขาว | `green-600` |
| hover ของปุ่มหลัก | `green-700` |
| ลิงก์บนพื้นขาว | `green-600` |
| ข้อความรองบนพื้นเขียวเข้ม | `green-100` |
| ไอคอน/เส้นบนพื้นเขียวเข้ม | `green-300` |
| พื้นอ่อน chip | `green-50` |

### สเกลเขียวมะนาว (ไล่จาก KU Lime)

`lime-50 #fcfde9` · `100 #f8fbc5` · `200 #f0f78c` · `300 #e4ee4a` · `400 #d2dd1c`
**`500 #b8c214` ← แบรนด์** · `600 #929b06` · `700 #6e7409` · `800 #575c0e` · `900 #4a4e12`

| ใช้ตรงไหน | Token |
|---|---|
| ตัวเลข/ไฮไลต์บนพื้นเขียวเข้ม | `lime-300` |
| focus ring (วงนอก) | `lime-300` |
| แถบเน้น จุดสถานะ | `lime-500` |
| **ข้อความ accent บนพื้นขาว** | `lime-700` ขึ้นไปเท่านั้น |

> ❌ **ห้ามใช้ `lime-500` เป็นสีข้อความบนพื้นขาว** คอนทราสต์ไม่ผ่าน
> ใช้เป็นพื้นหลังหรือแถบแทน แล้วเขียนตัวอักษรสี `green-950` ทับ

### กลาง

| Token | Hex | ใช้ทำอะไร |
|---|---|---|
| `paper` | `#ffffff` | พื้นหลัก |
| `cloud` | `#f1f6f4` | พื้นสลับ section พื้นกล่อง |
| `edge` | `#dce8e4` | เส้นขอบ เส้นแบ่ง |
| `ink` | `#0e2220` | ข้อความหลัก |
| `slate` | `#475a56` | ข้อความรอง (7.0:1 บนขาว เผื่อไว้สำหรับอ่านกลางแดด) |
| `obsidian` | `#000000` | **แถบ journey (`#how`) เท่านั้น** |

ทุกสีกลางมีเขียวผสมนิดนึง พื้นที่ว่างจะได้เป็นส่วนหนึ่งของแบรนด์
**ยกเว้น `obsidian`** ซึ่งเป็นตัวเดียวที่ไม่มีเขียวเลย

### แถบดำของ journey

เจ้าของสั่งให้ส่วนที่สอง (จากหน้าคุณ → ถึงรูปคุณ) ใหญ่ขึ้นและพื้นหลังเป็นสีดำ

ทำไมต้อง `#000000` ไม่ใช่ `green-950`: `green-950` คือ `#01211f` ซึ่งพอขยายเป็น
บล็อกเต็มความกว้างจะอ่านออกว่าเป็น**เขียวเข้ม** ไม่ใช่ดำ

> **นี่ไม่ได้ทำให้กฎสองสีในหัวข้อนี้แตก** กฎสองสีคุมสี*แบรนด์*
> `obsidian` เป็นสีกลางเหมือน `paper`/`cloud`/`edge` และเขียวกับไลม์ทุกตัวที่วางทับยังเหมือนเดิมทุกประการ

**สีตัวอักษรบนดำ** (วัดจริงแล้วทุกคู่):

| ใช้ตรงไหน | Token | อัตรา |
|---|---|---|
| หัวข้อ | `paper` | 21.0:1 |
| เนื้อความ | `green-100` | 16.6:1 |
| accent ตัวเลข ไฮไลต์ | `lime-300` | 16.6:1 |
| caption คำกำกับภาพสาธิต | `green-300` | 9.9:1 |

> ❌ **ห้ามใช้ `slate` บนดำ** วัดได้ 2.86:1 ตกทั้ง AA และ AA-large
> เคยพลาดมาแล้วหนึ่งครั้งที่ `figcaption` ของ `FaceScanDemo`

เส้นคั่นระหว่างส่วนที่ 1 กับ 2 เป็น `h-1 bg-lime-500` ไม่ใช่ `h-px bg-edge`
เพราะเส้นจาง 1px บนรอยต่อขาว→ดำ อ่านออกเป็นความผิดพลาดของการ render
ที่ความหนานี้มันคือแถบไลม์ใต้ตัวอักษร KU ในโลโก้ ซึ่งเป็นที่มาของสีนี้อยู่แล้ว

### พื้นหลังของทั้งเว็บ

พื้นขาวเปล่าถูกแทนด้วย **`<GridBackground variant="page" />`** ที่ `app/layout.tsx`
เป็นตารางเส้นเขียวจาง ๆ ที่เลื่อนช้า ๆ ตลอดเวลา

- ชั้นนี้เป็น `fixed` ขนาดเท่าจอ **ไม่ใช่ `absolute` เท่าความสูงเอกสาร**
  หน้ายาว ๆ อย่างแกลเลอรี 5,000 รูปจะได้ไม่ต้องแบกเลเยอร์สูงหลายจอโดยไม่ได้อะไรเพิ่ม
- ความเข้มเส้น **7%** ไม่ใช่ 12% แบบที่ใช้ในกล่องสถานะว่าง เพราะคราวนี้มีเนื้อความทับอยู่จริง
- section ที่มีพื้นของตัวเอง (แถบเขียว การ์ด กล่อง `cloud`) จะทับมันไปเอง
  ตารางเลยโผล่เฉพาะช่องว่างสีขาว ซึ่งคือที่ที่มันควรอยู่
- `prefers-reduced-motion` ทำให้ตารางหยุดนิ่งแต่ยังอยู่ — มันคือพื้นผิว ไม่ใช่ข้อมูล

**สว่างเสมอ ไม่มีโหมดมืด** เหตุผลคือฉากใช้งานจริง: ยืนกลางแดดเที่ยงในงานมหาลัย
พื้นขาวคอนทราสต์สูงอ่านง่ายที่สุดในสภาพนั้น

**`gray-*` `zinc-*` `slate-*` ของ Tailwind ไม่มีอยู่จริงในโปรเจกต์นี้**
`globals.css` ประกาศ `--color-*: initial` ลบ palette ดีฟอลต์ทิ้งทั้งชุด กฎนี้ถูกบังคับตอน build

### สัดส่วนบนหน้าจอ

ประมาณ **65 ขาว/cloud · 30 เขียวเข้ม · 5 เขียวมะนาว**
เขียวมะนาวคือเครื่องปรุง ไม่ใช่เนื้อ ถ้าเห็นเยอะกว่านี้แปลว่าใส่มากไป

---

## 3. ตัวอักษร

| บทบาท | ฟอนต์ | น้ำหนัก |
|---|---|---|
| Display | **K2D** | 600, 700 |
| Body | **Noto Sans Thai Looped** | 400, 500, 600 |

**ทำไม**

- **K2D** เรขาคณิตปลายมน มีบุคลิกขี้เล่นแบบไม่พยายาม รองรับไทย และไม่ใช่ Kanit/Prompt ที่เว็บไทยใช้กันเกลื่อน
- **Noto Sans Thai Looped** เป็นไทย**มีหัว** ซึ่งคนไทยอ่านแล้วรู้สึกเป็นมิตรกว่าไทยไม่มีหัว
  และอ่านง่ายที่สุดตอนตัวเล็กกลางแดด ซึ่งคือฉากใช้งานจริง
- **ไม่มีฟอนต์ mono** ตัวเลขใช้ body font + `font-variant-numeric: tabular-nums`
  ป้ายกำกับตัวพิมพ์ใหญ่แบบ mono เป็นลูกเล่นของนักออกแบบ ไม่ใช่ขนบของแอปผู้บริโภค

### สเกล — เป็น token จริง ห้ามพิมพ์ขนาดเอง

ทุกขั้นเป็น `--text-*` ใน `globals.css` และใช้ผ่านคลาสเดียว เช่น `text-h2`
ค่าเป็น `clamp()` ที่ไหลจากมือถือไปจอใหญ่เอง **ไม่ต้องใส่ `sm:` `lg:` กำกับขนาด**

| คลาส | มือถือ → จอใหญ่ |
|---|---|
| `text-display` | 2.25rem → 3.5rem |
| `text-h1` | 1.875rem → 2.75rem |
| `text-h2` | 1.5rem → 2.125rem |
| `text-h3` | 1.125rem → 1.375rem |
| `text-body-lg` | 1.0625rem → 1.1875rem |
| `text-body` | 0.9375rem → 1rem |
| `text-label` | 0.875rem |
| `text-caption` | 0.8125rem |

❌ **ห้ามเขียน `text-[2.25rem]` หรือ `text-2xl sm:text-4xl`** ถ้าขนาดที่ต้องการไม่มีในสเกล
แปลว่าสเกลผิด ให้แก้ token ไม่ใช่หลบด้วยค่าเฉพาะกิจ

น้ำหนักคุมด้วยคลาสแยก (`font-bold` = 700) โดยตอนนี้ base style อยู่ใน `@layer base`
แล้ว utility จึงชนะได้จริง — ก่อนหน้านี้ base อยู่นอก layer เลยกิน `font-bold` ทิ้งเงียบ ๆ

### กติกาภาษาไทย (สำคัญ)

```css
html[lang="th"] body          { line-height: 1.8; }
html[lang="th"] :is(h1,h2,h3) { letter-spacing: 0; line-height: 1.4; }
html[lang="en"] :is(h1,h2,h3) { letter-spacing: -0.02em; line-height: 1.12; }
```

- ❌ **ห้ามใส่ letter-spacing ติดลบกับข้อความไทย** วรรณยุกต์จะทับสระ
- ❌ **ห้ามใช้ `text-transform: uppercase` กับข้อความไทย** ไม่มีผลกับอักษรไทยและทำให้อังกฤษที่ปนดูตะโกน
- ตัวหนาไทยใช้ 600 ไม่ใช่ 700 (700 ทำให้หัวอักษรตัน) — 700 ใช้ได้เฉพาะหัวข้อใหญ่ที่เป็นอังกฤษหรือตัวเลข

---

## 4. รูปทรงและระยะ

### มุมโค้ง — มนใหญ่แบบแอปคนไทย

| Token | ค่า | ใช้กับ |
|---|---|---|
| `--radius-card` | 20px | การ์ด กล่อง แผง |
| `--radius-media` | 16px | รูป thumbnail |
| `--radius-field` | 14px | ช่องกรอก |
| `--radius-pill` | 999px | ปุ่ม chip แท็ก |

มุมเหลี่ยม 0px **ไม่มีที่ใช้แล้ว**

### ระยะ

ทวีคูณของ 4px · ที่ใช้บ่อย `8 12 16 20 24 32 40 48 64 80`

- ช่องไฟ section: `py-16` มือถือ → `py-24` จอใหญ่
- กว้างสุดของเนื้อหา: `max-w-6xl`
- ขอบซ้ายขวา: `px-5` → `px-8`
- ช่องไฟกริด: `gap-4` → `gap-6`

### สกอลบาร์

ทาสีเข้าแบรนด์ ไม่ปล่อยเป็นสีเทาของระบบปฏิบัติการ

| | ค่า |
|---|---|
| ความกว้าง | **12px** |
| แถบราง | โปร่งใส |
| ตัวเลื่อน | `green-400` มุมแคปซูล มีขอบโปร่งใส 3px ให้ลอยเป็นเม็ดยา |
| hover | `green-600` |
| กดค้าง | `green-700` |
| บนพื้นเขียวเข้ม | ใส่คลาส `.on-green` ตัวเลื่อนเปลี่ยนเป็น `lime-300` |

- **ห้ามทำให้บางกว่า 12px** แถบบางแบบที่กำลังฮิตจับยากจริง และเว็บนี้ถูกใช้บนคอมกลาง
  ที่นิสิตใช้ร่วมกัน กับบนมือถือที่ถือมือเดียวกลางฝูงคน ความสวยไม่คุ้มกับการจับพลาด
- `html` ตั้ง `scrollbar-gutter: stable` เสมอ ไม่งั้นเวลาย้ายจากหน้าสั้นไปหน้ายาว
  เนื้อหาที่จัดกลางจะกระตุกไปด้านข้างตอนแถบโผล่
- Firefox ปรับมุมโค้งไม่ได้ จึงใช้ความกว้างมาตรฐานของมันแล้วให้แค่สี
  **ห้ามใส่ `scrollbar-width: thin`** เพราะของ Firefox บางกว่า 12px และเสียเป้าที่จับ

### เงา — ใช้ได้ และควรใช้

ต่างจากระบบเดิมที่ห้ามเงา แอปผู้บริโภคใช้เงานุ่มเพื่อบอกว่ากดได้

| Token | ค่า | ใช้กับ |
|---|---|---|
| `--shadow-card` | `0 1px 2px rgb(14 34 32 / .04), 0 4px 16px -8px rgb(14 34 32 / .10)` | การ์ดปกติ |
| `--shadow-lift` | `0 2px 4px rgb(14 34 32 / .05), 0 12px 28px -12px rgb(14 34 32 / .18)` | การ์ดตอน hover |
| `--shadow-pop` | `0 8px 32px -8px rgb(0 112 107 / .35)` | ปุ่มหลัก |

---

## 5. คอมโพเนนต์

### ปุ่ม

แคปซูลเต็ม ตัวหนา สูงพอให้นิ้วกด

| แบบ | ปกติ | hover | active |
|---|---|---|---|
| `primary` | พื้น `green-600` ขาว เงา `shadow-pop` | `green-700` ยกขึ้น 1px | ยุบ 2% |
| `secondary` | พื้นขาว ขอบ `edge` ตัวหนังสือ `green-700` | พื้น `green-50` ขอบ `green-300` | ยุบ 2% |
| `ghost` | ไม่มีพื้น | พื้น `cloud` | ยุบ 2% |
| `danger` | พื้น `#c0392b` ขาว | มืดลง | ยุบ 2% |

ขนาด `sm` 38px · `md` 46px · `lg` 56px

### การ์ด

พื้นขาว มุม 20px เงา `shadow-card`
hover: ยก 3px + เงาเป็น `shadow-lift` + รูปข้างในซูม 4%

### Chip / แท็ก

แคปซูล พื้น `green-50` ตัวหนังสือ `green-700` ตัวเล็ก 500
ใช้บอกวันที่ สถานที่ จำนวนรูป — **สีเดียวทั้งหมด ไม่แยกสีตามหมวด**

### หัวข้อ section

หัวข้อใหญ่ + คำอธิบายหนึ่งบรรทัด **ไม่มีแถบขีด ไม่มี eyebrow ตัวพิมพ์ใหญ่**
ถ้าต้องการ eyebrow ใช้ตัวหนังสือปกติสี `green-600` น้ำหนัก 600

### แถบเขียวเต็มความกว้าง

พื้น `green-600` ตัวหนังสือขาว ข้อความรอง `green-100`
ตัวเลขลำดับใส่ในชิปกลม พื้น `lime-500` ตัวหนังสือ `green-950` — เป็นที่อยู่หลักของเขียวมะนาว
**ไม่เกินหนึ่งแถบต่อหนึ่งหน้า**

### สถานะกำลังโหลด

มี primitive อยู่ที่ `components/ui/loading.tsx` **ห้ามเขียน skeleton ดิบเองซ้ำ**

| ใช้อะไร | เมื่อไหร่ |
|---|---|
| `<Skeleton className="…" />` | บล็อกวิบวับหนึ่งก้อน กำหนดขนาดเอง ไม่รู้จักหน้าไหน ใช้ได้ทุกที่ |
| `<LoadingBar />` | แถบไม่รู้ความคืบหน้า ใช้ตอน**ไม่รู้รูปร่างหน้า** |
| `<LoadingRegion label>` | ห่อ UI โหลดทุกชุด ใส่ `aria-busy` + live region + ข้อความ sr-only ให้อัตโนมัติ |
| `<LoadingFallback label>` | ทั้งชุดสำเร็จรูปสำหรับหน้าที่รูปร่างไม่แน่นอน |
| `<Button pending>` | ปุ่มในฟอร์ม ได้ spinner + disabled + `aria-busy` ครบ |

**❌ ห้ามทำ `<PageLoading>` ที่วาดโครงหน้ากลาง ๆ ให้ทุกหน้าใช้**
เคยมีแล้วและเป็นบั๊ก — `loading.tsx` ตัวเดียววาดโครงหน้าแรกให้ 6 route
ห้าหน้าจึงแฟลชโครงที่ไม่มีวันมาถึง **skeleton ที่ผิดรูปแย่กว่าไม่มี skeleton**

**กติกา:** skeleton จะซื่อสัตย์ได้ก็ต่อเมื่ออยู่ติดกับหน้าที่มันเลียนแบบ
route ไหนรู้รูปร่างตัวเองแน่ ๆ ให้มี `loading.tsx` ของตัวเอง (`/events`, `/e/[slug]`)
route ที่เหลือใช้ `<LoadingFallback>` ซึ่งไม่สัญญารูปร่างอะไรเลย

**skeleton ของคอมโพเนนต์ให้อยู่ไฟล์เดียวกับตัวจริง** เช่น `EventCardSkeleton`
อยู่ใน `event-card.tsx` — แยกไฟล์เมื่อไหร่มันจะเลิกตรงกันเมื่อนั้นโดยไม่มีใครรู้

### สถานะว่าง

ต้องมี: พาดหัวสั้น + หนึ่งประโยคบอกว่าทำไมว่าง + ปุ่มถ้าผู้ใช้ทำอะไรได้
พื้นหลังใช้ `<GridBackground />` ห้ามใช้กล่องเทาเปล่า

### ช่องกรอกรหัสงาน

**6 ช่อง ช่องละ 1 ตัว** (`EventCodeInput`) รูปแบบรหัสนิยามไว้ที่ `lib/event-code.ts` ที่เดียว

- ตัวอักษรที่ใช้: `0-9` + `A-Z` **ยกเว้น I, L, O** เพราะบนป้ายที่งานมันคือรูปเดียวกับ 1, 1, 0
  ในเมื่อรหัสจริงมีสามตัวนั้นไม่ได้ การแปลงที่คนพิมพ์มาเป็นตัวเลขจึงไม่มีทางทำให้รหัสที่ถูกอยู่แล้วพัง
- ผ่อนปรนกับสิ่งที่ไม่มีความหมาย: พิมพ์เล็ก/ใหญ่, ช่องว่าง, ขีดคั่น
- **ยังรับลิงก์อยู่** วางลิงก์ลงช่องไหนก็ได้ → ส่งเข้า hidden `q` แล้ว submit เลย
  หกช่องช่องละตัวแสดง URL ไม่ได้ แต่คนที่ถือลิงก์มาต้องไปถึงงานให้ได้เหมือนกัน
- **ทำงานได้ตอน JS ไม่ทำงาน** แต่ละช่องส่ง `c0`…`c5` ของตัวเอง แล้ว server action ประกอบกลับเมื่อ `q` ว่าง
- a11y: ห่อด้วย `role="group"` + `aria-labelledby` และแต่ละช่องบอกตำแหน่งตัวเอง
  ไม่งั้น screen reader จะอ่านเป็นช่องกรอกหกช่องที่ไม่เกี่ยวอะไรกัน

> ⚠️ **ช่องไฟ `gap-1` บนมือถือไม่ใช่รสนิยม** 6 ช่องที่ 44px ตามเพดานหัวข้อ 9 กินไปแล้ว 264px
> จอ 360px เหลือให้ 288px ในการ์ด (ซึ่งต้องลด padding เป็น `px-4` ด้วย) ช่องไฟคือส่วนที่เหลือ
> วัดจริงบนมือถือ 360px ได้ 44.7px — ใส่มากกว่านี้แล้วกล่องจะหลุดเพดานทันที

### แถบ PDPA

แถบเดียวก่อน footer พื้น `green-50` หนึ่งประโยค + ลิงก์ ห้ามยาวกว่านี้

---

## 6. การเคลื่อนไหว — ขี้เล่นได้ในกรอบขนบ

Easing มาตรฐาน `cubic-bezier(0.16, 1, 0.3, 1)` (token `--ease-out`) — ตัวเดียว ใช้ทุกที่

ยังไม่มี easing แบบเด้ง/overshoot ในระบบ ตอนออกแบบเคยใส่ไว้เผื่อจังหวะ "เจอแล้ว!"
แต่ยังไม่มีหน้าไหนใช้จริง เลยลบทิ้งแทนที่จะปล่อยเป็นโทเคนลอย ๆ
ถ้าหน้าผลการค้นหาต้องการจังหวะดีใจจริง ค่อยเพิ่มตอนนั้นพร้อมเหตุผล — และต้องระวังว่า
overshoot ที่ใส่พร่ำเพรื่อทำให้เว็บดูเชย ใส่ได้เฉพาะจุดที่ฉลองอะไรบางอย่างจริง ๆ

| ที่ไหน | อะไร | นาน |
|---|---|---|
| ของเข้าหน้าจอ | ลอยขึ้น + จาง | 500ms |
| เลื่อนถึง section | เหมือนกัน ผ่าน `animation-timeline: view()` | ตาม scroll |
| ตัวเลขสถิติ | นับขึ้น | 1100ms |
| hover ปุ่ม | ยก 1px + สีเข้มขึ้น | 180ms |
| กดปุ่ม | ยุบ 2% | 100ms |
| hover การ์ด | ยก 3px + เงาลึกขึ้น + รูปซูม | 250ms / 600ms |
| เปลี่ยนหน้า | รูปปก morph ข้ามหน้า | 380ms |

### CSS หรือ anime.js — เลือกยังไง

โปรเจกต์นี้มี **anime.js v4** ติดตั้งไว้ แต่**ไม่ได้แปลว่าให้ใช้กับทุกอย่าง**

| ใช้ CSS | ใช้ anime.js |
|---|---|
| จังหวะรู้ล่วงหน้า | จังหวะขึ้นกับ network หรือข้อมูลจริง |
| ของตกแต่ง ที่ต้องขยับตั้งแต่ก่อน hydration | อยู่ใน client component อยู่แล้ว |
| จำนวน element คงที่ | จำนวน element มาจากผลลัพธ์ ไม่รู้ล่วงหน้า |
| ลูปที่ไม่ต้องหยุดกลางคัน | ต้องยกเลิกกลางคันโดยไม่กระตุก |

**ตัวอย่างที่ตัดสินไปแล้ว**

- **สาธิตสแกนหน้าใน hero → CSS** ไทม์ไลน์ 15 วินาทีเส้นเดียว การ์ดได้ช่วงละ 5 วิ
  ทุก element ใช้ duration เดียวกันต่าง delay จึงหลุดจากกันไม่ได้ และขยับตั้งแต่ก่อน JS โหลด
- **จอกำลังเทียบใบหน้า → anime.js** เพราะ Rekognition ตอบเมื่อไหร่ไม่รู้ ต้องวนจนกว่าคำตอบจะมา
  แล้วหยุดสะอาดทุกทางออก ซึ่ง CSS keyframe ยกเลิกกลางรอบแล้วจะกระตุก
- **ผลการค้นหาไล่กันขึ้น → anime.js** จำนวนรูปมาจากคำตอบ CSS `nth-child` ครอบไม่ได้

**anime.js ไม่สนใจ `prefers-reduced-motion` ให้เอง** ทุกจุดที่เรียกต้องเช็คก่อนเสมอ
และต้องออกแบบให้ "ไม่เล่นอนิเมชัน" = เห็นผลลัพธ์สุดท้ายครบ ห้ามมี element ที่ซ่อนอยู่จนกว่าจะ animate

### กติกาที่ห้ามพลาด

1. **`prefers-reduced-motion` ต้องครอบทุกตัว** รวม `::view-transition-*`
2. ❌ **ห้ามซ่อนเนื้อหาด้วย `opacity: 0` แล้วรอ JavaScript มาเปิด**
   ใช้ `animation-timeline: view()` ที่เป็น CSS ล้วน เบราว์เซอร์ที่ไม่รองรับเห็นเนื้อหาครบทันที
3. ค่าที่ JS จะ animate ต้อง render ค่าจริงจากเซิร์ฟเวอร์ไว้ก่อนเสมอ
4. อนิเมชันห้ามขวางการกดปุ่ม ผู้ใช้ยืนตากแดด รีบ

---

## 7. รูปภาพ

- thumbnail กริด 4:3 มุม 16px `object-cover`
- ทุกรูปเสิร์ฟผ่าน `/api/media/...` เท่านั้น ห้ามลิงก์ตรงไปที่ไฟล์
- ใช้ `<img>` ธรรมดา ไม่ใช้ `next/image` — เราสร้าง WebP ขนาดพอดีตอนอัปโหลดแล้ว
- รูปที่ยังไม่มี: `<GridBackground />` ห้ามใช้กล่องเทา

---

## 8. การเข้าถึง

ไม่มีมาตรฐานบังคับจากเจ้าของ ทำตามมาตรฐานวิชาชีพ

- **focus ring เป็นสองชั้น** เส้นเข้ม `green-900` ชิดขอบ + เส้น `lime-300` วงนอก
  เหตุผล: เขียวมะนาวบนขาวได้แค่ 1.95:1 ซึ่งตกเกณฑ์ WCAG 1.4.11 ที่ต้องการ 3:1
  บนพื้นขาวเส้นเข้มเป็นตัวแบกคอนทราสต์ บนแถบเขียวเส้นมะนาวเป็นตัวแบก มีอันหนึ่งผ่านเสมอ
- **ห้ามใส่ `border-radius` ใน `:focus-visible`** outline กับ box-shadow วิ่งตามมุมของ element เอง
  ถ้าไปกำหนดค่าไว้ ปุ่มแคปซูลกับการ์ดจะกลายเป็นเหลี่ยมทันทีที่กด Tab
- ปุ่มบนมือถือสูงอย่างน้อย 44px
- ข้อความบนพื้น `green-600` ใช้ได้แค่ ขาว · `green-100` (4.7:1) · `lime-300`
- ทุกหน้ามีลิงก์ข้ามไปเนื้อหา
- loading ต้องมี `aria-busy` + `aria-live`
- ออกแบบบนสมมติฐานว่าอ่านกลางแดด — คอนทราสต์เผื่อไว้

---

## 9. เขียนข้อความยังไง

- **พูดจากฝั่งผู้ใช้** "หารูปของฉัน" ไม่ใช่ "ค้นหาด้วย face recognition"
- **ปุ่มบอกว่าจะเกิดอะไร** "โหลดทั้งหมด" ไม่ใช่ "ตกลง"
- **คำเดิมตลอด flow** ปุ่มเขียน "ค้นหารูปของฉัน" หน้าถัดไปต้องเรียก "ผลการค้นหา"
- **error บอกวิธีแก้** "ไม่พบใบหน้าในรูปนี้ ลองรูปที่เห็นหน้าชัดกว่านี้"
- **หน้าว่างคือคำชวน ไม่ใช่การบ่น**
- เป็นกันเองได้ แต่ไม่กวน ไม่ใช้ emoji ในเนื้อหาระบบ
- ทุกข้อความต้องมีทั้ง th และ en ใน `lib/i18n/dictionaries.ts` — `en` ถูก type check กับ `th`

---

## 10. ห้ามกุขึ้นเอง

ยังไม่มีรูปจริง งานจริง ผู้ใช้จริง หรือรีวิว (ดู PRODUCT.md)
**ห้ามใส่**ชื่องานปลอม ชื่อช่างภาพปลอม จำนวนผู้ใช้ปลอม คำนิยมปลอม โลโก้พันธมิตรปลอม
ทุกตัวเลขบนหน้าเว็บมาจากฐานข้อมูลจริง และต้องอ่านรู้เรื่องตอนค่าเป็นศูนย์

---

## 11. เช็กลิสต์ก่อนสร้างหน้าใหม่

- [ ] โครงหน้าเป็นแบบที่คนคุ้นเคย ไม่ต้องเรียนรู้ใหม่
- [ ] มีสีแบรนด์แค่สองสี ไม่มีสีเสริมโผล่มา
- [ ] เขียวเต็มแถบไม่เกินหนึ่งแถบ
- [ ] เขียวมะนาวไม่เกิน 5% ของหน้าจอ
- [ ] ไม่มี `gray-*` `zinc-*` `slate-*`
- [ ] ไม่มีกรอบ AF / แถบขีด / พื้นมืด ที่ตกค้างจากระบบเดิม
- [ ] มุมโค้งตาม token
- [ ] ข้อความครบทั้ง th และ en
- [ ] มีสถานะว่างและสถานะ error
- [ ] `prefers-reduced-motion: reduce` แล้วเนื้อหายังครบ
- [ ] คีย์บอร์ดอย่างเดียวใช้ได้ครบ เห็น focus ทุกจุด
- [ ] จอกว้าง 360px ไม่มีอะไรล้น
